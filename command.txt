alembic revision --autogenerate -m "init Car model"
alembic upgrade head
rm alembic/versions/*.py
DELETE FROM alembic_version;


sudo -u postgres psql postgres
CREATE DATABASE auto_ria WITH OWNER alex ENCODING 'UTF8';
grant all privileges on database quiz_db to alex;
grant all on schema public to alex;


uvicorn app.main:app --reload


RUN docker compose:

docker compose build
docker compose up
если хочешь запуск в фоне: docker compose up -d

docker compose down

Пичистить все и Кеш:
docker system prune -a --volumes
docker image prune -a
docker volume prune



docker compose down
docker compose up --build

docker compose down -v

docker volume ls
docker volume rm autoria_postgres_data

